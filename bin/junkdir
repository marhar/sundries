#!/bin/sh
# junkdir -- creates a convenient junk directory under $HOME
cd $HOME
for i in 1 2 3 4 5 6 7 8 9; do
	j=$HOME/junk$i$i
	if test -d $j; then
		echo $j exists
	else
		echo mkdir $j
		mkdir $j
		exit 0
	fi
done
